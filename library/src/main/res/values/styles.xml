<resources xmlns:android="http://schemas.android.com/apk/res/android">

    <style name="TooltipAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>

    <!-- Holo default ActionButton (dimensions) -->
    <style name="HoloActionButton">
        <item name="android:minWidth">@dimen/action_button_min_width</item>
        <item name="android:minHeight">@dimen/action_bar_default_height</item>
    </style>

</resources>
